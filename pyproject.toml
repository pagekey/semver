# TODO Build tools

[tool.poetry]
name = "pagekey-semver"
description = "Simple, opinionated semantic versioning package."
version = "0.1.0"
authors = [
    "steve"
]

[tool.poetry.scripts]
pagekey-semver = "pagekey_semver.main:cli_entrypoint"

[tool.poetry.dependencies]
python = ">=3"

[tool.poetry.groups.dev.dependencies]
pytest = "*"
